package settings

import (
	"net/url"
	"time"
)

// LegacySettings configures the legacy Bitcoin wire protocol service.
type LegacySettings struct {
	WorkingDir                       string        `key:"legacy_workingDir" desc:"Working directory for legacy operations" default:"../../data" category:"Legacy" usage:"Path for legacy data storage" type:"string" longdesc:"### Purpose\nSpecifies the directory where the legacy Bitcoin protocol service stores its operational data.\n\n### How It Works\nThis directory contains:\n- **Peer database** - Known legacy node addresses\n- **Orphan transactions** - Transactions waiting for parent inputs\n- **Temporary files** - Protocol processing artifacts\n\n### Options\n- **Relative path** - Resolved relative to binary location\n- **Absolute path** - Full filesystem path\n\n### Recommendations\n- Use absolute paths in production for predictability\n- Ensure sufficient disk space for orphan transaction storage\n- Place on fast storage for better performance"`
	ListenAddresses                  []string      `key:"legacy_listen_addresses" desc:"Listen addresses for legacy Bitcoin protocol (pipe-separated)" default:"" category:"Legacy" usage:"For backward compatibility with older nodes" type:"[]string" longdesc:"### Purpose\nConfigures network addresses where the node accepts incoming connections from legacy Bitcoin protocol clients.\n\n### How It Works\nEnables backward compatibility with:\n- BSV Blockchain nodes not yet upgraded to Teranode\n- Legacy mining software\n- Traditional Bitcoin protocol tools\n\n### Options\n- **ip:port format** - Multiple addresses separated by pipes\n- **Empty string** - Disables legacy protocol listening entirely\n\n### Examples\n| Value | Description |\n|-------|-------------|\n| 0.0.0.0:8333 | Listen on all interfaces |\n| 192.168.1.10:8333 | Listen on specific IP |\n\n### Recommendations\n- Leave empty unless you need to connect with legacy nodes\n- Use firewall rules to restrict access in production"`
	ConnectPeers                     []string      `key:"legacy_connect_peers" desc:"Legacy peers to connect to (pipe-separated)" default:"" category:"Legacy" usage:"For syncing with legacy Bitcoin nodes" type:"[]string" longdesc:"### Purpose\nSpecifies legacy Bitcoin node addresses that this node should actively connect to as outbound peers.\n\n### How It Works\nThe node initiates connections to these addresses on startup and maintains persistent connections for:\n- Blockchain synchronization with legacy nodes\n- Transaction relay to legacy infrastructure\n- Block announcement propagation\n\n### Options\n- **ip:port format** - Multiple addresses separated by pipes\n- **Empty string** - No outbound legacy connections\n\n### Use Cases\n- Syncing with existing BSV Blockchain infrastructure\n- Connecting to known trusted legacy nodes\n- Private network setups with legacy components\n\n### Recommendations\n- Only connect to trusted legacy nodes\n- Monitor connection health in logs"`
	OrphanEvictionDuration           time.Duration `key:"legacy_orphanEvictionDuration" desc:"Duration before evicting orphan transactions" default:"10m" category:"Legacy" usage:"How long to keep orphan transactions" type:"duration" longdesc:"### Purpose\nControls how long orphan transactions are retained before being automatically removed from memory.\n\n### How It Works\nOrphan transactions are transactions received where one or more parent inputs are unknown. The node holds these temporarily hoping the parent transactions arrive soon.\n\nAfter this duration, orphans are evicted to:\n- Prevent memory exhaustion\n- Clean up transactions that will likely never complete\n- Protect against orphan flooding attacks\n\n### Trade-offs\n- **Longer duration** - More time for parent transactions to arrive, but uses more memory\n- **Shorter duration** - Less memory usage, but may prematurely evict valid orphans\n\n### Recommendations\n- Default of 10 minutes works well for most networks\n- Reduce on memory-constrained systems\n- Increase if experiencing frequent orphan re-requests"`
	MaxOrphanTxs                     int           `key:"legacy_maxOrphanTxs" desc:"Maximum number of orphan transactions held in memory" default:"100" category:"Legacy" usage:"Hard cap on the orphan transaction pool" type:"int" longdesc:"### Purpose\nBounds the size of the orphan transaction pool to prevent memory-exhaustion DoS from a peer flooding unique orphan transactions faster than the TTL eviction rate.\n\n### How It Works\nWhen the orphan pool reaches this cap, inserting a new orphan evicts the oldest entry first (by insertion time). The evicted orphan is given one final validation attempt, mirroring TTL-based eviction semantics.\n\n### Recommendations\n- Default of 100 matches Bitcoin Core's historical orphan pool size.\n- Raise only if you have reason to expect legitimate bursts of orphans larger than the default and have memory headroom.\n- Setting to 0 disables the cap (legacy unbounded behaviour) and is not recommended on public networks."`
	StoreBatcherSize                 int           `key:"legacy_storeBatcherSize" desc:"Batch size for legacy store operations" default:"1024" category:"Legacy" usage:"Number of operations per batch" type:"int" longdesc:"### Purpose\nDefines the number of storage operations grouped together in a single batch when processing legacy protocol messages.\n\n### How It Works\nInstead of writing each item individually, operations are collected into batches:\n- Reduces storage round trips\n- Improves throughput for bulk data\n- Amortizes overhead across multiple operations\n\n### Trade-offs\n- **Larger batches** - Better throughput, higher memory usage, increased latency per item\n- **Smaller batches** - Lower latency, more storage overhead, reduced throughput\n\n### Recommendations\n- Default of 1024 balances throughput and memory well\n- Increase for high-volume legacy traffic\n- Decrease if memory is constrained"`
	StoreBatcherConcurrency          int           `key:"legacy_storeBatcherConcurrency" desc:"Concurrency for legacy store batcher" default:"32" category:"Legacy" usage:"Parallel store batch operations" type:"int" longdesc:"### Purpose\nControls the number of parallel workers processing storage batch operations for the legacy protocol.\n\n### How It Works\nMultiple workers can process independent batches simultaneously, enabling:\n- Parallel writes to storage backends\n- Better utilization of multi-core systems\n- Higher aggregate throughput\n\n### Trade-offs\n- **Higher concurrency** - Increased throughput, more resource usage, potential storage contention\n- **Lower concurrency** - Reduced resource usage, lower throughput\n\n### Recommendations\n- Default of 32 works well for most deployments\n- Scale with available CPU cores and storage backend capacity\n- Monitor storage backend for signs of contention"`
	SpendBatcherSize                 int           `key:"legacy_spendBatcherSize" desc:"Batch size for legacy spend operations" default:"1024" category:"Legacy" usage:"Number of spends per batch" type:"int" longdesc:"### Purpose\nDefines the number of UTXO spend operations grouped together in a single batch when processing legacy protocol transactions.\n\n### How It Works\nWhen transactions from the legacy protocol spend UTXOs, the operations are batched for efficiency:\n- Multiple spends processed together\n- Reduced UTXO store round trips\n- Improved validation throughput\n\n### Trade-offs\n- **Larger batches** - Better throughput for high-volume scenarios\n- **Smaller batches** - Lower latency for individual transactions\n\n### Recommendations\n- Keep aligned with **StoreBatcherSize** for consistent behavior\n- Default of 1024 is suitable for typical workloads"`
	SpendBatcherConcurrency          int           `key:"legacy_spendBatcherConcurrency" desc:"Concurrency for legacy spend batcher" default:"32" category:"Legacy" usage:"Parallel spend batch operations" type:"int" longdesc:"### Purpose\nControls the number of parallel workers processing UTXO spend batch operations for the legacy protocol.\n\n### How It Works\nMultiple workers process spend batches concurrently, enabling:\n- Parallel UTXO lookups and updates\n- Faster transaction validation\n- Better utilization of UTXO store capacity\n\n### Trade-offs\n- **Higher concurrency** - Faster processing, more UTXO store load\n- **Lower concurrency** - Less resource contention, slower throughput\n\n### Recommendations\n- Keep aligned with **StoreBatcherConcurrency** for consistent behavior\n- Default of 32 balances performance and resource usage"`
	OutpointBatcherSize              int           `key:"legacy_outpointBatcherSize" desc:"Batch size for legacy outpoint operations" default:"1024" category:"Legacy" usage:"Number of outpoints per batch" type:"int" longdesc:"### Purpose\nDefines the number of outpoint lookup operations grouped together in a single batch when processing legacy protocol requests.\n\n### How It Works\nOutpoints (transaction output references) are looked up in batches for efficiency:\n- Multiple outpoint queries combined\n- Reduced UTXO store round trips\n- Improved lookup throughput\n\n### Trade-offs\n- **Larger batches** - Better throughput for bulk lookups\n- **Smaller batches** - Lower latency for individual queries\n\n### Recommendations\n- Keep aligned with other batcher sizes for consistent behavior\n- Default of 1024 is suitable for typical workloads"`
	OutpointBatcherConcurrency       int           `key:"legacy_outpointBatcherConcurrency" desc:"Concurrency for legacy outpoint batcher" default:"32" category:"Legacy" usage:"Parallel outpoint batch operations" type:"int" longdesc:"### Purpose\nControls the number of parallel workers processing outpoint lookup batch operations for the legacy protocol.\n\n### How It Works\nMultiple workers process outpoint lookup batches concurrently, enabling:\n- Parallel UTXO queries\n- Faster transaction input resolution\n- Better utilization of UTXO store capacity\n\n### Trade-offs\n- **Higher concurrency** - Faster lookups, more UTXO store load\n- **Lower concurrency** - Less resource contention, slower throughput\n\n### Recommendations\n- Keep aligned with other batcher concurrency settings\n- Default of 32 balances performance and resource usage"`
	PrintInvMessages                 bool          `key:"legacy_printInvMessages" desc:"Print inventory messages for debugging" default:"false" category:"Legacy" usage:"Enable for legacy protocol debugging" type:"bool" longdesc:"### Purpose\nEnables verbose logging of inventory (inv) messages received through the legacy Bitcoin protocol.\n\n### How It Works\nWhen enabled, every inventory message is logged including:\n- Transaction announcements from peers\n- Block announcements\n- Other inventory vectors\n\n### Options\n- **true** - Log all inventory messages\n- **false** - Suppress inventory message logging\n\n### Trade-offs\n- **Enabled** - Useful for debugging protocol issues, generates high log volume\n- **Disabled** - Cleaner logs, less visibility into protocol traffic\n\n### Recommendations\n- **Disable in production** - Very verbose output\n- Enable temporarily when troubleshooting legacy peer connectivity\n- Use in conjunction with log rotation when enabled"`
	GRPCAddress                      string        `key:"legacy_grpcAddress" desc:"gRPC address for Legacy service" default:"" category:"Legacy" usage:"Address for legacy service communication" type:"string" longdesc:"### Purpose\nSpecifies the gRPC address used by other Teranode services to communicate with the legacy service.\n\n### How It Works\nOther microservices connect to this address to:\n- Request data from legacy peers\n- Coordinate transaction propagation\n- Exchange blockchain state information\n\n### Options\n- **host:port format** - Full address for the gRPC endpoint\n- **Empty string** - Legacy service gRPC client disabled\n\n### Recommendations\n- Configure when running distributed multi-service deployments\n- Use internal network addresses for security\n- Ensure firewall allows traffic between services"`
	AllowBlockPriority               bool          `key:"legacy_allowBlockPriority" desc:"Enable the BSV multistream BlockPriority stream policy for legacy P2P" default:"true" category:"Legacy" usage:"Negotiate multiple TCP streams per peer, separating block traffic from general traffic" type:"bool" longdesc:"### Purpose\nEnables the BSV multistream protocol (BlockPriority stream policy) for legacy P2P connections. Peers negotiate multiple TCP streams per association, separating bulk block/header transfer (DATA1) from latency-sensitive control traffic (GENERAL).\n\n### How It Works\n- Advertised in the protoconf handshake; peers that also support BlockPriority open a DATA1 stream\n- Block and header replies are delivered on DATA1, isolating multi-GB transfers from control messages\n- Required for reliable download of large (multi-GB) blocks from BSV mainnet sync peers\n\n### Options\n- **true** - Negotiate BlockPriority multistream with capable peers (default)\n- **false** - Single-stream legacy protocol only\n\n### Recommendations\n- Keep enabled for mainnet/testnet sync, where large blocks require the dedicated block stream"`
	GRPCListenAddress                string        `key:"legacy_grpcListenAddress" desc:"gRPC listen address for Legacy service" default:"" category:"Legacy" usage:"Port for incoming legacy gRPC connections" type:"string" longdesc:"### Purpose\nSpecifies the address where the legacy service listens for incoming gRPC connections from other Teranode services.\n\n### How It Works\nThe legacy service exposes its internal API on this address, allowing other microservices to:\n- Query legacy peer state\n- Submit transactions for legacy propagation\n- Access legacy-specific functionality\n\n### Options\n- **host:port format** - Address to bind the gRPC server\n- **Empty string** - Legacy gRPC server disabled\n\n### Recommendations\n- Bind to internal network interfaces only\n- Use **0.0.0.0:port** for container deployments\n- Pair with **GRPCAddress** for bidirectional communication"`
	SavePeers                        bool          `key:"legacy_savePeers" desc:"Persist legacy peer list to disk" default:"false" category:"Legacy" usage:"Enable for peer persistence across restarts" type:"bool" longdesc:"### Purpose\nControls whether discovered legacy peer addresses are saved to disk for use across node restarts.\n\n### How It Works\nWhen enabled:\n- Peer addresses discovered during operation are written to disk\n- On restart, saved peers are loaded immediately\n- Reduces time needed to establish network connectivity\n\nPeer data is stored in the **WorkingDir** location.\n\n### Options\n- **true** - Persist peers to disk\n- **false** - Peers discovered fresh each startup\n\n### Trade-offs\n- **Enabled** - Faster startup, requires disk write permissions\n- **Disabled** - Clean slate each start, no stale peer data\n\n### Recommendations\n- Enable for production nodes with stable peer networks\n- Disable for ephemeral/test deployments\n- Periodically clean peer database to remove stale entries"`
	AllowSyncCandidateFromLocalPeers bool          `key:"legacy_allowSyncCandidateFromLocalPeers" desc:"Allow sync candidate from local peers" default:"false" category:"Legacy" usage:"Enable for local network sync" type:"bool" longdesc:"### Purpose\nControls whether peers on local/private networks are considered as blockchain sync candidates.\n\n### How It Works\nBy default, the node excludes local network peers (RFC1918 addresses) from sync candidate selection as a security measure. When enabled:\n- Local network peers can be chosen for initial block download\n- Private testnet setups work correctly\n- Development environments can sync without public peers\n\n### Options\n- **true** - Local network peers eligible for sync\n- **false** - Only public network peers eligible for sync\n\n### Use Cases\n- Private/isolated blockchain networks\n- Development and testing environments\n- Air-gapped deployments\n\n### Recommendations\n- **Keep disabled for mainnet** - Prevents local network attacks\n- Enable only for private networks and testing\n- Combine with **ConnectPeers** for controlled sync sources"`
	TempStore                        *url.URL      `key:"temp_store" desc:"Temporary storage URL for legacy operations" default:"file://./data/tempstore" category:"Legacy" usage:"Temporary data storage location" type:"url" longdesc:"### Purpose\nSpecifies the storage location for temporary data generated during legacy protocol operations.\n\n### How It Works\nTemporary storage is used for:\n- Intermediate block processing data\n- Transaction validation scratch space\n- Protocol message buffering\n\n### Options\n- **file://path** - Local filesystem storage\n- Other URL schemes supported by the blob store\n\n### Examples\n| Value | Description |\n|-------|-------------|\n| file://./data/tempstore | Relative path (default) |\n| file:///var/teranode/temp | Absolute path |\n\n### Recommendations\n- Use fast storage (SSD) for better performance\n- Ensure sufficient space for block processing\n- Configure automatic cleanup or use tmpfs for ephemeral data"`
	PeerIdleTimeout                  time.Duration `key:"legacy_peerIdleTimeout" desc:"Timeout for idle legacy peers" default:"125s" category:"Legacy" usage:"Disconnect after this idle time (ping interval is 2 mins)" type:"duration" longdesc:"### Purpose\nDefines the maximum time a legacy peer connection can remain idle before being disconnected.\n\n### How It Works\nThe Bitcoin protocol uses a 2-minute ping/pong mechanism to detect dead connections. This timeout:\n- Starts counting from last message received\n- Resets on any incoming protocol message\n- Triggers disconnection when exceeded\n\n### Trade-offs\n- **Higher timeout** - More tolerant of network issues, slower detection of dead peers\n- **Lower timeout** - Faster cleanup of dead connections, may disconnect slow peers prematurely\n\n### Recommendations\n- Default of 125 seconds provides buffer above 2-minute ping interval\n- Account for network jitter and latency\n- Increase for high-latency network conditions\n- Do not set below 120 seconds (ping interval)"`
	MaxAddnodePeers                  int           `key:"legacy_maxAddnodePeers" desc:"Maximum peers connected via addnode, budgeted separately from MaxPeers" default:"8" category:"Legacy" usage:"Ceiling on named (addnode) peers, independent of the automatic outbound target" type:"int" longdesc:"### Purpose\nNamed peers are budgeted separately from the automatic outbound tier, so configuring them never costs the node one of its independently chosen outbound addresses. That separation needs its own ceiling, or an addnode list could grow the node without limit.\n\n### How It Works\n- Bounds the addnode list only; connect-only peers (**legacy_connect_peers**) are the node's entire connectivity and are never capped\n- Additive to **MaxPeers**, which bounds the automatic outbound and inbound tiers, matching svnode where the addnode semaphore is sized independently of maxconnections\n- Enforced both on the startup list and on the runtime addnode RPC\n\n### Recommendations\n- Leave at **8**, matching svnode's -maxaddnodeconnections default"`
	MaxFeelerPeers                   int           `key:"legacy_maxFeelerPeers" desc:"Peer slots reserved for short-lived feeler probes; 0 disables feelers entirely" default:"1" category:"Legacy" usage:"Budget for probes that verify addresses the node is not otherwise dialling" type:"int" longdesc:"### Purpose\nThe node only ever learns that an address works as a side effect of a connection it wanted anyway, so the pool of addresses known to be reachable decays. A feeler is a short-lived probe: it connects to an address the node is not using, completes the version exchange to prove somebody is home, and hangs up. The address is then marked as verified, so when a peer is later lost the replacement is drawn from addresses that answered recently.\n\n### How It Works\n- The reservation is taken out of the peer-admission ceiling, not out of the automatic outbound target, so probing never costs the node a real outbound peer. svnode does the same arithmetic, subtracting its feeler budget from inbound capacity (net.cpp:1261)\n- The value is also the hard cap on probes running at once\n- Probes only start once the automatic outbound tier is already at its target\n\n### Options\n- **1** (default) — matches svnode, which runs a single feeler\n- **0** — disables the feature completely: no probes, and no slot reserved\n\n### When the node reserves nothing regardless\nTwo further conditions switch feelers off on their own, and both log the reason at startup:\n- **Connect-only mode** (**legacy_connect_peers** set) — the node's whole connectivity is the configured list and it stops discovering peers for itself, so a verified address has nothing to feed\n- **A cap too tight to hold the reservation** — if reserving would push the peer-admission ceiling below the automatic outbound target, the probe is given up rather than the tier, because a node in that state would sit permanently below target, dialling and being refused by its own door. Note the shipped cap is **legacy_config_MaxPeers = 20** against a target of 8, so there is ample room; lowering that cap towards the target is what brings this into play\n\n### Recommendations\n- Leave at **1**. The reservation costs one inbound slot out of the peer cap, and buys an address book whose entries have been checked."`
	FeelerInterval                   time.Duration `key:"legacy_feelerInterval" desc:"Average gap between feeler probes" default:"120s" category:"Legacy" usage:"Mean of the randomised delay between probes" type:"duration" longdesc:"### Purpose\nPaces the feeler probe. Matches svnode's FEELER_INTERVAL of 120 seconds (net.h:88).\n\n### How It Works\nThe gap between probes is randomised around this mean rather than fixed. A fixed period would be a fingerprint: an observer seeing probes exactly two minutes apart can recognise the node across address changes and predict the next one. A randomised gap leaks neither. svnode randomises the same way, in PoissonNextSend.\n\n### Options\n- **120s** (default) — svnode's cadence, about thirty probes an hour\n- **Shorter** — verifies the address book faster, useful when testing\n- **Below 1s** — accepted but not honoured: the probe loop only checks its deadline once a second, so the realised gap never falls below a second and the realised mean of a sub-second setting is higher than the one configured\n- **0 or negative** — falls back to 120s with a warning. This is deliberately NOT a disable lever; use **legacy_maxFeelerPeers = 0** for that, so a node cannot end up with feelers switched off while still paying for the reserved slot\n\n### Recommendations\n- Leave at **120s** outside testing."`
	FeelerHandshakeTimeout           time.Duration `key:"legacy_feelerHandshakeTimeout" desc:"How long a feeler probe waits for a version message" default:"25s" category:"Legacy" usage:"Must be shorter than the 30s peer negotiate timeout so a mute host is hung up by the feeler, not logged as a lost peer" type:"duration" longdesc:"### Purpose\nBounds how long a feeler waits for the far side to identify itself before hanging up.\n\n### How It Works\nAssociateConnection starts the peer package's own 30s negotiate timer within microseconds of the probe starting this one, so an equal or longer feeler deadline cannot be relied on to fire first. Whichever fires first owns the teardown. If the peer package wins, on a host that accepts TCP and then stays silent, the disconnect is logged at warning and counted as a lost peer; the feeler hangs up quietly instead. The deadline therefore has to leave the peer timer a clear margin.\n\n### Options\n- **25s** (default) — five seconds inside the peer timeout\n- **Shorter** — useful when testing\n- **0 or negative** — falls back to 25s with a warning\n- **30s or more** — reduced to 29s with a warning; at or above the peer timeout the peer package hangs the connection up first, at warning level\n\n### Recommendations\n- Leave at **25s** outside testing."`
	ReplenishInterval                time.Duration `key:"legacy_replenishInterval" desc:"How often the connection manager tops outbound peers back up to the target" default:"2s" category:"Legacy" usage:"Connection supply cadence; faster replenish closes the gap left by a disconnect" type:"duration" longdesc:"### Purpose\nEvery disconnect leaves the node below its outbound target until the connection manager notices and dials a replacement. A slow replenish cadence turns each disconnect into a measurable stretch of reduced download capacity — visible during IBD as the 'quiet' half of the busy/quiet oscillation.\n\n### How It Works\n- On each tick the connection manager compares live outbound peers against the target and dials the shortfall\n- Faster ticks mean the shortfall is closed in seconds rather than tens of seconds\n- A freed outbound slot also wakes the loop directly, so a lost peer is usually replaced within about a second\n\n### Options\n- **2s** (default) — connection supply tracks demand closely\n- **0** — rollback lever: restores the historical fixed one-minute cadence and disables the event-driven wake\n\n### Recommendations\n- Leave at **2s**; the cost of a faster tick is a cheap periodic comparison, the benefit is that connection supply tracks demand closely"`
	PeerProcessingTimeout            time.Duration `key:"legacy_peerProcessingTimeout" desc:"Timeout for processing legacy peer messages" default:"3m" category:"Legacy" usage:"Maximum time for processing block messages" type:"duration" longdesc:"### Purpose\nDefines the maximum time allowed to process a single legacy protocol message before timing out.\n\n### How It Works\nSome protocol messages, particularly large blocks, can take significant time to process. This timeout:\n- Applies per-message during processing\n- Prevents indefinite hangs on malformed data\n- Protects against slow-loris style attacks\n\n### Trade-offs\n- **Higher timeout** - Accommodates larger blocks, slower detection of stuck processing\n- **Lower timeout** - Faster failure detection, may timeout on legitimate large blocks\n\n### Recommendations\n- Default of 3 minutes handles most block sizes\n- Increase for networks with very large blocks\n- Monitor for timeout errors in logs to adjust\n- Consider hardware performance when tuning"`
	BlockFailureBackoffBase          time.Duration `key:"legacy_blockFailureBackoffBase" desc:"Base wait before re-attempting a block after a transient storage/service failure" default:"5s" category:"Legacy" usage:"Linear backoff unit, multiplied by the consecutive failure count and capped at legacy_blockFailureBackoffMaxDuration" type:"duration" longdesc:"### Purpose\nWhen processing a legacy-sync block fails with a transient storage/service error (e.g. Aerospike overload surfacing as a client timeout), the same block is re-delivered and would otherwise immediately re-run the full multi-million-record decorate at full concurrency, re-loading an already struggling node. This sets the base per-block backoff: re-deliveries of a failed block hash are cheaply skipped until the backoff window elapses.\n\n### How It Works\nBackoff grows linearly with the consecutive failure count (base, 2xbase, 3xbase, ...) and is capped at legacy_blockFailureBackoffMaxDuration. The skip is non-blocking — the block handler returns a retryable error rather than sleeping, so the single block-processing goroutine is never stalled.\n\n### Values\n- **Default 5s** - quick first retry, backs off further on repeated failure\n- **0** - disables the per-block backoff (re-attempt immediately, prior behaviour)\n\n### Related\nComplements the Aerospike server-side overload retry (#1085), which handles DEVICE_OVERLOAD/MAX_ERROR_RATE but deliberately not client TIMEOUT. This block-level backoff (#1187) sits one layer up and throttles whole-block re-decorate storms."`
	BlockFailureBackoffMaxDuration   time.Duration `key:"legacy_blockFailureBackoffMaxDuration" desc:"Cap on the per-block backoff window and the failure-tracking map TTL" default:"150s" category:"Legacy" usage:"Also bounds how long a failed block hash is remembered before its failure count resets" type:"duration" longdesc:"### Purpose\nCaps the linear per-block backoff window (see legacy_blockFailureBackoffBase). It bounds only the retry SPACING; the failure-tracking map TTL is deliberately longer (this cap plus legacy_peerProcessingTimeout), so the consecutive-failure count survives one full slow failing attempt and the linear ramp actually engages on the overload path (a single failing outpoint decorate can outlast this cap, and a TTL equal to the cap would expire the entry mid-attempt and reset the count to its base every time).\n\n### Values\n- **Default 150s** - bounded retry cadence for a persistently failing block; long enough that a transient overload clears between attempts, and deliberately kept below the 180s sync-peer stall window (maxLastBlockTime) so a backed-off block reliably clears its window before the stall detector would rotate the delivering peer\n- Should be >= legacy_blockFailureBackoffBase to be meaningful\n- Keep below the 180s stall window; a larger value risks a rotated-in peer re-delivering a still-backed-off block\n- **0 (or any value <= 0)** - disables the per-block backoff entirely (the tracking map is not created)\n\n### Note\nWhen enabled, memory is bounded two ways: entries self-evict after the map TTL (this cap + legacy_peerProcessingTimeout), and the tracking map has a hard size cap, so only currently-failing block hashes are held."`

	BlockDownloadTimeoutBasePercent    int64 `key:"legacy_blockDownloadTimeoutBasePercent" desc:"Wall-clock ceiling on one block download at the chain tip, as a percentage of the target block interval" default:"100" category:"Legacy" usage:"100 means one block interval" type:"int64" longdesc:"### Purpose\nCaps how long a single block transfer may be kept alive by throughput-based deadline extension. Expressed as a percentage of the target block interval rather than an absolute duration, so the same value is correct on any chain regardless of its block time. Mirrors svnode's -blockdownloadtimeoutbasepercent.\n\n### How It Works\nceiling = blockInterval * (base + perPeer * otherPeersDownloading) / 100. At the tip the base is this value; while catching up it is BlockDownloadTimeoutBaseIBDPercent instead.\n\n### Recommendations\n- **100** (default) matches svnode. On mainnet that is 10 minutes."`
	BlockDownloadTimeoutBaseIBDPercent int64 `key:"legacy_blockDownloadTimeoutBaseIBDPercent" desc:"Wall-clock ceiling on one block download while catching up, as a percentage of the target block interval" default:"600" category:"Legacy" usage:"600 means six block intervals" type:"int64" longdesc:"### Purpose\nDuring initial block download a historical block is large and our own validation backpressure delays the read loop, so the tip-sized ceiling aborts honest transfers just short of completion and forces a full re-download from another peer — the most expensive failure mode in a sync, because it wastes the bandwidth already spent.\n\n### How It Works\nReplaces BlockDownloadTimeoutBasePercent in the ceiling calculation whenever the sync manager reports that we are catching up. At the tip the original value applies unchanged, so a dead tip peer is still reaped promptly.\n\n### Recommendations\n- **600** (default) matches svnode. On mainnet that is 60 minutes.\n- Set equal to BlockDownloadTimeoutBasePercent to restore a single ceiling everywhere."`
	BlockDownloadTimeoutPerPeerPercent int64 `key:"legacy_blockDownloadTimeoutPerPeerPercent" desc:"Extra ceiling allowed per other peer we are downloading blocks from, as a percentage of the target block interval" default:"50" category:"Legacy" usage:"Compensates for our own downstream link being shared" type:"int64" longdesc:"### Purpose\nWhen blocks are being pulled from several peers at once each transfer is legitimately slower, because our own downstream link is shared between them. Without compensation, fetching in parallel makes every peer look stalled and gets them all disconnected. Mirrors svnode's -blockdownloadtimeoutperpeerpercent.\n\n### How It Works\nAdds this percentage to the ceiling for each OTHER peer with a block download in flight. Only peers with a genuine outstanding request are counted, so a peer cannot inflate our patience by advertising blocks it does not have.\n\n### Recommendations\n- **50** (default) matches svnode.\n- **0** disables the compensation and restores a flat ceiling."`

	BlockPrefetchBufferBytes int64 `key:"legacy_blockPrefetchBufferBytes" desc:"Memory budget (bytes) for blocks downloaded ahead of processing during legacy sync. 0 disables prefetch (synchronous ingestion)." default:"268435456" category:"Legacy" usage:"Bounds the total serialized bytes of received-but-not-yet-processed blocks across all legacy peers" type:"int64" longdesc:"### Purpose\nDecouples the per-peer network read-loop from per-block processing during legacy/IBD sync so block download overlaps validation, while bounding the memory held by blocks that have been received but not yet processed.\n\n### How It Works\nThe read-loop normally blocks until each block finishes processing, so download never overlaps processing (the next block's ~seconds-long download is serialized after the previous block's validation). With a non-zero budget the read-loop instead admits a block against a global byte-weighted semaphore and continues reading the next block while the previous one is validated in the background. The budget is the hard ceiling on the total serialized size of in-flight (received, queued, or mid-validation) blocks across ALL legacy peers and streams. A single block larger than the whole budget is admitted alone (it waits for everything else to drain first), preserving the original one-block-at-a-time backpressure for huge blocks.\n\n### Options\n- **0** - Disable prefetch; fall back to the original synchronous ingestion (one block in flight, full TCP backpressure). Use as a kill switch.\n- **>0** - Enable bounded async prefetch with this byte budget.\n\n### Trade-offs\n- **Larger budget** - More download/processing overlap and higher small-block throughput, more resident memory for buffered blocks.\n- **Smaller budget** - Less memory, less overlap; at/below one block's size it degrades to synchronous behaviour.\n\n### Recommendations\n- Default of 256 MiB hides the inter-block download gap in the small-block era while keeping buffered-block memory well bounded.\n- The weight is a block's serialized size; the live decoded object graph is a small multiple, so size the budget below the headroom you actually have.\n- Big-block era: a block at least as large as the whole budget is admitted alone (its weight is clamped to the budget), giving zero download/validation overlap — identical to pre-prefetch behaviour. To get overlap on large blocks, raise the budget to at least ~2x the typical block size so more than one is in flight.\n- Set to 0 to revert to proven synchronous behaviour if a regression is suspected."`

	MaxBlockParallelFetch int           `key:"legacy_maxBlockParallelFetch" desc:"Maximum number of peers allowed to be downloading the same stalled frontier block at once" default:"2" category:"Legacy" usage:"1 disables racing; 2 means the original peer plus one extra copy" type:"int" longdesc:"### Purpose\nDuring headers-first sync the node commits blocks strictly in order, so the oldest block it has asked for and not yet received — the download frontier — holds up everything behind it. If the peer that owes us that one block goes quiet, sync stops until the 180-second sync-peer stall timer fires, disconnects that peer and throws away the whole downloaded header list.\n\nThis setting caps how many peers may be downloading that one frontier block at the same time. When the frontier has been stuck for longer than legacy_blockSlowFetchTimeout, and the sync peer is not visibly pulling bytes, the node asks one additional peer for the same block while leaving the original request in place. Whichever copy arrives first is used; the other is discarded without penalising the peer that sent it.\n\n### Values\n- **1** - disables racing entirely; only ever one peer is asked for a block (behaviour before this setting existed)\n- **2 (default)** - the original peer plus at most one extra copy\n- **>2** - more copies in parallel, one added per five-second check, at the cost of one duplicate block's bandwidth per extra peer\n\n### Trade-offs\nEach extra racer costs a full duplicate download of one block if the original peer eventually answers too. In exchange a silently dropped request costs roughly 20-30 seconds instead of three minutes plus a lost peer and a full header re-download. Only the frontier block is ever raced, never the rest of the queue.\n\n### Note\nRacing only happens during headers-first sync below the last hardcoded checkpoint. On a node that is already synced there is no frontier and this setting does nothing."`
	BlockSlowFetchTimeout time.Duration `key:"legacy_blockSlowFetchTimeout" desc:"How long the download frontier may sit unchanged before a second peer is asked for the same block" default:"20s" category:"Legacy" usage:"Age at which a stuck frontier block becomes eligible for parallel fetch" type:"duration" longdesc:"### Purpose\nSets how long the oldest outstanding block may go undelivered before the node asks an additional peer for the same block (see legacy_maxBlockParallelFetch).\n\n### How It Works\nA five-second timer checks the frontier. It is only treated as stuck if all of the following hold: the frontier hash has not changed for this long, the node is not throttling its own reads because local validation is behind, and the sync peer's connection is not currently pulling bytes at a healthy rate — a peer part-way through a multi-gigabyte block is slow, not stalled, and must not be raced.\n\n### Values\n- **Default 20s** - well below the 180-second sync-peer rotation window, leaving a wide band in which racing can actually help\n- **0 or negative** - disables racing entirely\n\n### Important\nThis must stay comfortably below the 180-second sync-peer stall window. Set close to or above it and the peer is rotated (and the header list discarded) before a race would ever be attempted, which makes the feature unreachable rather than merely conservative."`

	MultiPeerBlockDownload bool `key:"legacy_multiPeerBlockDownload" desc:"Download block bodies from every eligible peer at once during legacy sync, instead of only from the sync peer" default:"true" category:"Legacy" usage:"false restores the single-sync-peer download path" type:"bool" longdesc:"### Purpose\nDuring headers-first sync the node already knows every block hash it needs up to the next checkpoint, but historically it asked one peer — the sync peer — for all of them. Download speed was then one peer's upload rate down one TCP connection, however many peers were connected. With this on, the header list is divided into contiguous runs and each run is asked of a different eligible peer, which is how svnode has always done it (svnode has no sync peer for block bodies at all).\n\n### How It Works\nA peer is eligible if it is a connected sync candidate and its advertised best height is at or above the block being handed out. Eligible peers are ordered with the sync peer first and each is given a contiguous run of the header list, bounded by legacy_maxBlocksInTransitPerPeer per peer and legacy_blockDownloadWindow across the node. A hash is only ever assigned to one peer; the one exception is the stalled-frontier race (legacy_maxBlockParallelFetch), which deliberately asks a second peer for the single block holding up sync.\n\n### Options\n- **true (default)** - block bodies come from every eligible peer\n- **false** - one getdata to the sync peer, sized by the block-size ladder, exactly as before this setting existed\n\n### Note\nOut-of-order arrival becomes the normal case rather than the exception, so this depends on the out-of-order block park (legacy_parkOutOfOrderBlocks). With the park off, blocks that arrive ahead of their parent are discarded and re-requested, and fanning out makes that far more frequent."`

	MaxBlocksInTransitPerPeer int `key:"legacy_maxBlocksInTransitPerPeer" desc:"Maximum block bodies one peer may be downloading at once during legacy sync" default:"16" category:"Legacy" usage:"svnode uses a hardcoded 16; the block-size ladder lowers it further for large blocks" type:"int" longdesc:"### Purpose\nCaps how deep one peer's block queue may get, which is what stops a single peer being handed the whole header list while three other peers sit idle. svnode's equivalent, MAX_BLOCKS_IN_TRANSIT_PER_PEER, is a hardcoded 16.\n\n### How It Works\nThe node's own block-size ladder — 20 in flight for blocks under 100MB, stepping down to 1 above 2GB — is applied on top as a ceiling, so this figure governs small blocks and the ladder governs large ones. At the ladder's lowest rungs it also narrows the fan-out itself, so a stretch of multi-gigabyte blocks collapses back to one peer with one block in flight.\n\n### Values\n- **16 (default)** - svnode's depth\n- **lower** - shallower queues, more even spread across peers, more round trips\n- **higher** - deeper queues per peer; each block in flight is a block that will be decoded and held in memory when it arrives, so raise it with legacy_blockPrefetchBufferBytes and actual RSS in view\n\n### Note\nOnly applies while legacy_multiPeerBlockDownload is on. With it off, the block-size ladder alone governs the sync peer's queue depth."`

	BlockDownloadWindow int `key:"legacy_blockDownloadWindow" desc:"Maximum block bodies the whole node may be downloading at once during legacy sync" default:"1024" category:"Legacy" usage:"svnode uses DEFAULT_BLOCK_DOWNLOAD_WINDOW of 1024; counts blocks owed by every peer together" type:"int" longdesc:"### Purpose\nThe node-wide ceiling on outstanding block requests. Where legacy_maxBlocksInTransitPerPeer bounds one peer, this bounds the sum over every peer, so adding peers cannot grow the queue without limit. svnode's equivalent is DEFAULT_BLOCK_DOWNLOAD_WINDOW, also 1024.\n\n### How It Works\nBefore each pass the node counts the blocks it is already owed and only hands out the difference. At the default per-peer depth of 16 and the default of 8 outbound peers the per-peer caps bind first (8 x 16 = 128), so this acts as a backstop rather than the everyday limit; lower it to put a hard node-wide floor under the fan-out.\n\n### Values\n- **1024 (default)** - svnode's window\n- **lower** - a hard ceiling on how many blocks may be outstanding across all peers\n\n### Note\nThis bounds requests, not memory. Memory is bounded by legacy_blockPrefetchBufferBytes, which is applied as each block is read off the wire; only legacy_multiPeerBlockDownload reads this setting."`

	ParkOutOfOrderBlocks bool `key:"legacy_parkOutOfOrderBlocks" desc:"Write a block whose parent is not stored yet to the temp store and commit it when the parent arrives, instead of discarding it" default:"true" category:"Legacy" usage:"Keeps out-of-order blocks instead of throwing them away" type:"bool" longdesc:"### Purpose\nDuring headers-first sync a block whose parent is not in the chain yet cannot be committed. Today it is fully downloaded, fully decoded and then thrown away, and the getblocks the node sends instead is ignored while headers-first mode is on — so the download is simply wasted and the walk never comes back to that block.\n\n### How It Works\nWith this on, such a block is checked (proof of work, merkle root, non-empty transaction list) and then streamed to the temp blob store. When its parent commits, the block is read back off disk and committed in order, and the blob is deleted. Nothing is held in memory beyond a small index entry.\n\nThe getblocks that answers a missing parent is sent whether the block is kept or dropped. Keeping the block preserves the download; the getblocks fetches the gap, and in the legacy protocol it is also the signal without which the peer sends nothing more.\n\nThe peer answers that getblocks with an inventory list covering the gap, which includes the blocks already parked. The inventory path asks the park as well as the chain, so a block already on disk is recognised and not downloaded a second time — without that the park would save the decode and none of the bandwidth outside headers-first mode.\n\n### Options\n- **true (default)** - keep out-of-order blocks on disk, and still ask for the missing parent.\n- **false** - no park at all: log, send a getblocks, discard the block, write nothing to disk.\n\n### What This Switch Does NOT Turn Off\nIt turns the park off; it does not turn off the other changes that shipped alongside it. In particular, in headers-first mode a discarded block still has the download walk put back onto it, so it is asked for again — a separate fix, deliberately not gated here, because without it a single out-of-order block stops headers-first sync until the sync-peer stall detector fires. Outside headers-first mode there is no walk and nothing is rewound, which is exactly the behaviour that came before.\n\n### Note\nThe park needs a temp store it can enumerate on restart, so it switches itself off with a warning on any temp_store URL that is not a file:// store."`

	ParkMaxBytes int64 `key:"legacy_parkMaxBytes" desc:"Ceiling on the total serialized bytes of out-of-order blocks held on disk. 0 disables the park" default:"4294967296" category:"Legacy" usage:"Disk budget for out-of-order blocks awaiting their parent" type:"int64" longdesc:"### Purpose\nBounds the disk the park may use, in bytes rather than in blocks, because a block count means something completely different at 11 KB per block than at 150 MB per block.\n\n### How It Works\nA block that would take the total past this figure is refused; nothing already parked is evicted to make room, because the oldest parked block is the one closest to being committable. A refused block is dropped and re-requested exactly as if it had never arrived.\n\n### Values\n- **4 GiB (default)** - about 27 blocks at 150 MB, and far more small blocks than the in-memory index cap of 4096 allows.\n- **0** - disables the park entirely, independently of legacy_parkOutOfOrderBlocks, for an operator who is simply out of disk."`

	ParkStoreTimeout time.Duration `key:"legacy_parkStoreTimeout" desc:"Deadline carried by each park blob store operation, bounding the wait for the file store's shared permits on the in-order block commit goroutine" default:"10s" category:"Legacy" usage:"Bounds head-of-line blocking on the block-queue consumer" type:"duration" longdesc:"### Purpose\nThe park's blob store work runs on the one goroutine that commits blocks in order, so time spent in it delays every block queued behind. The file store shares 256 write permits and 768 read permits process-wide with subtree writes, transaction writes and both persisters, and waits up to 25 seconds for one. A caller deadline is the only thing that can shorten that wait.\n\n### What It Bounds\nEvery blob store operation the park makes carries this deadline, one at a time:\n- the write that parks a block,\n- the read that fetches it back when its parent lands,\n- the delete that releases its blob afterwards,\n- the header peek the restart scan makes over each recovered blob.\n\nSo committing one parked block costs at most two of these, and parking one costs at most one. On expiry the operation is abandoned and the outcome is always survivable, and never at the cost of the download: a timed-out write records nothing and the block is re-requested (nothing reached the disk to lose), a timed-out read KEEPS the block parked and leaves the download walk alone, because running out of permits says nothing about the block and the sweep will try it again, and a timed-out delete forgets the entry and leaves the file for the next restart sweep.\n\n### What It Does NOT Bound\nThe work the store does once it holds a permit. The deadline is consulted while waiting for one of the shared permits (the store caps that wait at 25 seconds of its own); the open, copy and unlink that follow do not consult a context at all, so on a hung mount this setting buys nothing.\n\nThe stateless check a block passes before it is written — proof of work, and rebuilding the merkle tree over every transaction — is CPU work on that same goroutine, and no context deadline can interrupt it part way through. Its cost is set by the block, not by a clock: milliseconds for a small block, seconds for a multi-gigabyte one. When it runs longer than this setting the park logs a warning naming the block, its transaction count and the time taken, so an operator watching the commit goroutine stall can tell validation from store contention.\n\n### Values\n- **10s (default)** - bounds the worst-case store delay to one block commit while still riding out a normal burst of contention for permits.\n- Values are clamped to a floor of one second; the park as a whole is switched off with the two settings above, not with this one."`
	Upnp             bool          `key:"legacy_upnp" desc:"Enable UPnP for automatic port mapping" default:"false" category:"Legacy" usage:"Enable automatic port forwarding via UPnP" type:"bool" longdesc:"### Purpose\nEnables Universal Plug and Play (UPnP) for automatic port forwarding on compatible routers.\n\n### How It Works\nWhen enabled, the node will:\n- Attempt to discover UPnP-capable routers on the local network\n- Automatically configure port forwarding for incoming connections\n- Periodically renew the port mapping lease\n\n### Options\n- **true** - Enable UPnP port mapping\n- **false** - Disable UPnP (manual port forwarding required)\n\n### Trade-offs\n- **Enabled** - Easier setup, requires UPnP support on router\n- **Disabled** - More secure, requires manual router configuration\n\n### Recommendations\n- Keep disabled in production for security\n- Enable for home/development setups behind NAT\n- Ensure router supports UPnP if enabled"`
}
