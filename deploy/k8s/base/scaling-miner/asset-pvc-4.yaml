apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: asset-pvc-4
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: io2-xfs-low-iops
  resources:
    requests:
      storage: 1000Gi
