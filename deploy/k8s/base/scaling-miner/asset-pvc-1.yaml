apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: asset-pvc-1
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: io2-xfs-low-iops
  resources:
    requests:
      storage: 1000Gi
