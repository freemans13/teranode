apiVersion: traefik.containo.us/v1alpha1
kind: IngressRouteTCP
metadata:
  name: ingressroute-tcp-8284
  labels:
    service: propagation
spec:
  entryPoints:
    - prop-frpc
  routes:
  - match: HostSNI(`*`)
    services:
    - name: propagation-service
      port: 8284
