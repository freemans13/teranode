apiVersion: traefik.containo.us/v1alpha1
kind: IngressRouteTCP
metadata:
  name: ingressroute-tcp-8091
  labels:
    service: asset
spec:
  entryPoints:
    - ast-grpc
  routes:
    - match: HostSNI(`*`)
      services:
        - name: asset-service
          port: 8091
