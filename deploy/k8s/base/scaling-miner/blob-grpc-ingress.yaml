apiVersion: traefik.containo.us/v1alpha1
kind: IngressRouteTCP
metadata:
  name: m1-ingressroute-tcp-8091

spec:
  entryPoints:
    - blob-grpc
  routes:
  - match: HostSNI(`m1.scaling-miner.ubsv.dev`)
    services:
    - name: blob-service
      port: 8091
  tls:
    # use m1-cert
    passthrough: true
