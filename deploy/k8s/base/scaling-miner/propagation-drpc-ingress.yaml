apiVersion: traefik.containo.us/v1alpha1
kind: IngressRouteTCP
metadata:
  name: ingressroute-tcp-8184
  labels:
    service: propagation
spec:
  entryPoints:
    - prop-drpc
  routes:
  - match: HostSNI(`m1.scaling.ubsv.dev`)
    services:
    - name: propagation-service
      port: 8184
  tls:
    passthrough: true
