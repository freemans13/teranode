apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: propagation-luster-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: propagation-luster-pv
  resources:
    requests:
      storage: 2400Gi

