apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: tcp-8091-asset-miner
  labels:
    service: miner
spec:
  entryPoints:
    - ast-grpc
  routes:
    - match: HostSNI(`*`)
      services:
        - name: miner
          port: 8091
  