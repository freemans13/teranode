apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: coinbase-ingressroute-tcp-8093
  labels:
    service: miner
spec:
  entryPoints:
    - coin-grpc
  routes:
    - match: HostSNI(`*`)
      services:
        - name: miner
          port: 8093
