apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: kafka-ingress-9094
  labels:
    service: kafka
spec:
  entryPoints:
    - kafka-external
  routes:
    - match: HostSNI(`*`)
      services:
        - name: kafka-external
          port: 9094
