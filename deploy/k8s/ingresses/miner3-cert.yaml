apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: miner3-cert
  namespace: miner3
spec:
  secretName: miner3-tls
  dnsNames:
  - miner3.ubsv.dev
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
